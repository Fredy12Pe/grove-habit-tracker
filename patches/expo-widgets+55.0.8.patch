diff --git a/node_modules/expo-widgets/scripts/autolinking.rb b/node_modules/expo-widgets/scripts/autolinking.rb
index 9f8531d..59e91bc 100644
--- a/node_modules/expo-widgets/scripts/autolinking.rb
+++ b/node_modules/expo-widgets/scripts/autolinking.rb
@@ -6,9 +6,9 @@ require File.join(File.dirname(`node --print "require.resolve('expo-modules-auto
 require File.join(File.dirname(`node --print "require.resolve('react-native/package.json')"`), "scripts/react_native_pods")
 
 def use_expo_modules_widgets!(options = {})
-  output = Expo::AutolinkingManager.new(self, @current_target_definition, options).resolve
-
-  all_packages = output["modules"].map { |mod| mod["packageName"] }
+  # SDK 54+ makes AutolinkingManager#resolve private; resolve runs inside #initialize.
+  manager = Expo::AutolinkingManager.new(self, @current_target_definition, options)
+  all_packages = manager.instance_variable_get(:@packages).map(&:name)
   used_packages = ["expo", "expo-widgets", "@expo/ui"]
   packages_to_exclude = all_packages - used_packages
 
diff --git a/node_modules/expo-widgets/scripts/build-bundle.mjs b/node_modules/expo-widgets/scripts/build-bundle.mjs
index afaa72c..ad2a8ec 100755
--- a/node_modules/expo-widgets/scripts/build-bundle.mjs
+++ b/node_modules/expo-widgets/scripts/build-bundle.mjs
@@ -50,7 +50,6 @@ const result = await spawn(
     path.join(__dirname, '../bundle/index.ts'),
     '--dev',
     'false',
-    '--skip-server',
     ...argv.slice(2),
   ],
   {
